export const DEFAULT_USER = {
  userId: "demo-user",
  userName: "本地用户",
};

export const RUNTIME_DIR = "data/runtime";
export const SHARES_FILE = `${RUNTIME_DIR}/shares.json`;
export const WALLET_FILE = `${RUNTIME_DIR}/wallet.json`;
export const SHARE_SUCCESS_FILE = `${RUNTIME_DIR}/share-success.json`;
export const DAILY_SCHEDULE_FILE = `${RUNTIME_DIR}/daily-schedule.json`;
